---
title: Data Maker for developers
description: Submit records, render forms, and read the schema — build your own Data Maker clients and renderers.
template: splash
hero:
  tagline: Submit records, render forms, and read the schema. Everything you need to build your own DataMaker client or renderer.
  actions:
    - text: Getting started
      link: /fobo-data-maker/getting-started/
      variant: primary
    - text: Schema reference
      link: /fobo-data-maker/schema/form-model/
      variant: secondary
---

## What this is

A DataMaker form is distributed as a signed **`.dmf`** bundle. Anyone can build
tooling around it: submit records to a form, render a form for people to fill
in, or read its schema to do something entirely new. Submissions are
**end-to-end encrypted** — only the form owner can read them.

<div class="fobo-grid">
  <div class="fobo-card">
    <i class="fa-solid fa-paper-plane"></i>
    <h3>Submit from any language</h3>
    <p>Official SDKs for <strong>JavaScript/Node</strong>, <strong>Python</strong>, and <strong>.NET</strong> read a <code>.dmf</code>, validate values, sealed-box encrypt, and post — no DataMaker app required.</p>
  </div>
  <div class="fobo-card">
    <i class="fa-solid fa-display"></i>
    <h3>Render anywhere</h3>
    <p>Drop a form on the web with the JS renderer, the <strong>WordPress</strong> plugin, or fill it in a <strong>terminal</strong>. Client-side or server-side encryption.</p>
  </div>
  <div class="fobo-card">
    <i class="fa-solid fa-puzzle-piece"></i>
    <h3>Build your own</h3>
    <p>The <code>.dmf</code> format, the submission wire contract, and the full form schema are documented so you can write a client or renderer in any stack.</p>
  </div>
  <div class="fobo-card">
    <i class="fa-solid fa-lock"></i>
    <h3>End-to-end encrypted</h3>
    <p>Values are sealed against the recipient's public key (libsodium sealed box). The server only routes ciphertext — it never sees your data.</p>
  </div>
</div>

## Pick your path

- **I have a `.dmf` and want to submit records** → an [SDK](/fobo-data-maker/sdks/javascript/).
- **I want to show a form on my site** → the [web embed](/fobo-data-maker/renderers/web-embed/)
  or [WordPress plugin](/fobo-data-maker/renderers/wordpress/).
- **I want to build something new** → [the schema reference](/fobo-data-maker/schema/form-model/)
  and [build your own](/fobo-data-maker/build-your-own/).
